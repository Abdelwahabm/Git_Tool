git config --global user.name "sqw"
git config --global user.email "ewd@gma.com"
git init
git add -A
git commit -m "ananan"
git remote add origin https://github.com/Abdelwahabm/Git_Tool
git push -u -f origin master
